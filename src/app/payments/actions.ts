'use server';

// TODO: Implement payments with Supabase and Paystack

export async function confirmReceipt(formData: FormData) {
  return {
    error: 'Receipt confirmation not implemented with Supabase yet',
    success: false,
  };
}

'use server';

// TODO: Implement notifications with Supabase

export async function saveFCMToken(formData: FormData) {
  return {
    error: 'FCM token saving not implemented with Supabase yet',
    success: false,
  };
}
